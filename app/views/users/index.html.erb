
 <div class="container">
	 <div class="row">
	 	<div class="col-xs-3">
			<%= render 'users/info',user: current_user %>
			<%= render 'books/form', book: @book %>
		</div>
		<div class="col-xs-9">
		<h1>Users</h1>
		<%= render 'index', users: @users %>
		</div>
	</div>
</div>

